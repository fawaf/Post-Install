# It's kind of like `cat`, but with syntax highlighting
# consider example_code.rb, run
# pyg example_code.rb

sudo apt-get install python-pygments

echo 'alias pyg="pygmentize -f terminal"' >> ~/.bashrc
